---
- hosts: all
  tasks:
    - name: Test if release scripts exists
      stat:
        path: "{{ release['scripts_path'] }}/release.sh"
      register: release

    - fail: msg="Whoops! release script dos not exist"
      when: release.stat.isfile is defined and release.stat.isfile
